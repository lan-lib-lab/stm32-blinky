#ifndef UNIT_TEST_H
#define UNIT_TEST_H

#include "trap_error.h"   // require defintiions for module error codes

// defined unit tests to run and whether they're enabled
void utest_stk(void);
#define UTEST_STK_ENABLED 

#endif // UNIT_TEST_H
